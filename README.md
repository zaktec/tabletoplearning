# TableTopLearning

TableTopLearning is a static educational website for structured online courses and tutor-supported learning. It helps learners and parents browse subjects, understand the learning process, compare support plans and find contact information.

Live website: <https://zaktec.github.io/tabletoplearning/>

## Project status

The current repository contains a working public-facing Astro website. It is a static marketing and course-discovery experience, not a complete learning platform.

Implemented:

- Responsive homepage and navigation
- Eight subject landing pages
- Statically generated course detail pages
- Bronze, Silver and Gold plan comparisons
- Interactive FAQ disclosures
- About, contact, privacy and safeguarding pages
- GitHub Pages continuous deployment

Not implemented:

- Registration or authentication
- Learner dashboards and saved progress
- Course delivery or lesson content
- Enrolment, checkout or payments
- Tutor messaging or scheduling
- Database, API, CMS or admin panel
- Form-processing or email backend

The learner login page is currently an informational placeholder. Contact and policy actions use `mailto:` links.

## Technology stack

| Area | Technology |
| --- | --- |
| Framework | Astro 5 |
| Rendering | Static site generation |
| Language | Astro templates, TypeScript and browser JavaScript |
| Styling | Plain global CSS |
| Package manager | npm |
| CI runtime | Node.js 22 |
| Hosting | GitHub Pages |
| Automation | GitHub Actions |

Astro is the only runtime dependency. TypeScript and `@astrojs/check` are development dependencies.

## Application architecture

The active application is under `src/`:

```text
src/
├── layouts/
│   └── Layout.astro          Shared HTML document, header, navigation and footer
├── pages/
│   ├── index.astro           Homepage
│   ├── courses.astro         Featured-course overview
│   ├── courses/[slug].astro  Generated course detail pages
│   └── subjects/*/           Subject landing pages
└── styles/
    └── global.css            Design tokens, components and responsive styles
```

`Layout.astro` imports the global stylesheet and supplies the common metadata, navigation, footer and mobile-menu behaviour. The homepage contains a second small browser script for FAQ controls. There is no client-side framework or server-side runtime.

The older root-level `index.html`, `pages/`, `css/`, `scripts/` and `images/` paths are legacy files. They are not imported by Astro and are not included in the deployed Pages artifact. Changes to the live website should normally be made under `src/`.

## Public routes

General routes:

- `/tabletoplearning/`
- `/tabletoplearning/about/`
- `/tabletoplearning/contact/`
- `/tabletoplearning/courses/`
- `/tabletoplearning/login/`
- `/tabletoplearning/privacy/`
- `/tabletoplearning/safeguarding/`

Subject routes:

- Mathematics
- Computer Science
- AI & Machine Learning
- English
- Biology
- Physics
- Chemistry
- Religion & Languages

Course pages are generated by `src/pages/courses/[slug].astro`. Course information is stored in its `courseData` record, while `getStaticPaths()` declares which slugs Astro builds. Both must be updated together when adding a course.

## Content storage

There is no CMS or central data service. Content is currently stored directly in Astro files:

- Homepage pathways, course features, plans, audience guidance and FAQs: `src/pages/index.astro`
- Subject course lists: each `src/pages/subjects/*/index.astro`
- Course detail data: `src/pages/courses/[slug].astro`
- Shared navigation and metadata: `src/layouts/Layout.astro`
- Brand and design rules: `docs/branding.md`

Subject and course information is duplicated in several templates. When changing a course, check the homepage pathways, subject page, `courseData`, `getStaticPaths()` and all links to that course.

## Local development

Requirements:

- Node.js 22 recommended
- npm 9.6.5 or newer

Install the locked dependencies and start Astro:

```bash
npm ci
npm run dev
```

Astro prints the local development URL in the terminal.

Available commands:

| Command | Purpose |
| --- | --- |
| `npm run dev` | Start the development server |
| `npm start` | Alias for the development server |
| `npx astro check` | Run Astro and TypeScript diagnostics |
| `npm run build` | Generate the production site in `dist/` |
| `npm run preview` | Preview a production build locally |

Before publishing a public-facing change, run:

```bash
npx astro check
npm run build
```

## GitHub Pages base path

This repository is deployed as a GitHub Pages **project site**, so it is hosted below `/tabletoplearning/` rather than at the domain root.

`astro.config.mjs` contains:

```js
export default defineConfig({
  site: 'https://zaktec.github.io',
  base: '/tabletoplearning',
});
```

Internal cross-page links must use Astro's configured base path. Existing pages normalize it like this:

```ts
const base = `${import.meta.env.BASE_URL.replace(/\/$/, '')}/`;
```

Example:

```astro
<a href={`${base}about`}>About</a>
```

Do not introduce hard-coded root-relative paths such as `/about`, `/courses` or `/images/logo.png`. They would escape the project base and fail on GitHub Pages.

## Deployment

`.github/workflows/deploy.yml` deploys the website when code is pushed to `main`. It can also be started manually from GitHub Actions.

The workflow:

1. Checks out the repository.
2. Uses Node.js 22.
3. Runs `npm ci`.
4. Runs `npm run build`.
5. Uploads `dist/` as a GitHub Pages artifact.
6. Deploys with `actions/deploy-pages`.

The repository's Pages source must remain configured for GitHub Actions.

## Branding and accessibility

Public-facing work must follow [docs/branding.md](docs/branding.md). Important requirements include:

- Use the name **TableTopLearning** exactly.
- Prefer **learner** over **student**.
- Reuse the existing CSS tokens and components.
- Preserve keyboard controls, visible focus states and semantic elements.
- Preserve the responsive menu and reduced-motion behaviour.
- Keep the experience concise and readable on mobile.

## Documentation for developers and AI tools

- [Technical reference and AI context](docs/technical-reference.md): complete architecture, route, data, styling and deployment reference.
- [Project overview](docs/project-overview.md): product purpose, users, capabilities and boundaries.
- [Branding guide](docs/branding.md): visual and content standards.
- [Repository instructions](AGENTS.md): mandatory rules for coding agents.

When giving this repository to an AI assistant, provide `AGENTS.md`, `docs/technical-reference.md` and `docs/branding.md` first. Tell the assistant to treat `src/` as the active app, preserve the `/tabletoplearning/` base path and not assume that authentication, payments, a backend, a database or a CMS already exist.

## Contribution checklist

Before submitting changes:

- Confirm the change belongs in the active Astro app.
- Follow the brand guide for any public content or styling.
- Keep internal links compatible with the GitHub Pages base.
- Reuse existing layouts, classes and design tokens.
- Update duplicated course data consistently.
- Preserve responsive and keyboard behaviour.
- Run `npx astro check`.
- Run `npm run build`.
